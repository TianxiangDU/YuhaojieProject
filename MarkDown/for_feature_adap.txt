动机:
deformable conv用处不大如果只是在一层使用一次,而且不能顾忌到小物体的检测

解决方案:
去掉dcn而采用3x3 conv with mask(比重)

理由如下:
(1) 因为dcn和普通3x3卷积差别不大(实验对比而得),所以可以减少学习offset的时间
(2) 根据predicted shape,独立学习比重,从而保持了对大物体的优势,同时面对小物体时,减少有害信息的加入
